PUBLIC_SITE_URL=http://127.0.0.1:4321
# Empty = content/portfolio.json. Set only AFTER publishing the CMS globals.
CMS_URL=
# Browser-visible CMS origin, used only to refresh current CV metadata.
PUBLIC_CMS_URL=
# Optional server-only read API key (never use PUBLIC_ for this token).
CMS_READ_TOKEN=
